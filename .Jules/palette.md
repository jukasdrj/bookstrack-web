## 2024-05-23 - FilterChip Semantics
**Learning:** Using the `avatar` property in `FilterChip` is semantically better and cleaner than embedding icons in the `label` Row. It ensures consistent sizing (24x24 box) and simplifies the widget tree.
**Action:** Always prefer `avatar` property for icons in Chips.
